--To Check the version of GIT

# git --version

------------------------------------------------------------------------------------------------------------------------------------------------------

--To get any help regarding the GIT

# git help config
		or
# git config --help

------------------------------------------------------------------------------------------------------------------------------------------------------

--To configure the name and email 

# git config --global user.name 'Username'

# git config --global user.email 'Email-Id'

------------------------------------------------------------------------------------------------------------------------------------------------------

--To check the username and gmail 

# git config --list --show-origin

------------------------------------------------------------------------------------------------------------------------------------------------------

--To Create the file in the Git Bash Use

# touch filename.file-extension

------------------------------------------------------------------------------------------------------------------------------------------------------
--To Intialize GIT 

# git init
------------------------------------------------------------------------------------------------------------------------------------------------------

--To check .git file in the folder 

# goto View-> options-> Change folder and search options-> View -> check Show hidden files,folders and drives and un-check Hide extensions for known file types

------------------------------------------------------------------------------------------------------------------------------------------------------

--Local Repository create the files and add the files

# git add -A   //add all the files in the folder

# git add filename //will add that particular file in the folder

# git add *.txt //add all txt files

------------------------------------------------------------------------------------------------------------------------------------------------------

--To check the status of the files

# git status 

------------------------------------------------------------------------------------------------------------------------------------------------------

--To remove the added file 

# git rm --cached filename.extension

------------------------------------------------------------------------------------------------------------------------------------------------------

To commit the file 

git commit -m "commit message"

------------------------------------------------------------------------------------------------------------------------------------------------------

Remote Repository 


--To add the existing repository 

 # git remote add origin repository_name
------------------------------------------------------------------------------------------------------------------------------------------------------

--To know the remote name

# git remote
------------------------------------------------------------------------------------------------------------------------------------------------------

--To push the files to the remote repository 

# git push -u origin master

------------------------------------------------------------------------------------------------------------------------------------------------------

--To pull the changes 

# git pull

------------------------------------------------------------------------------------------------------------------------------------------------------

--To create the new branch 

# git branch branch_name

------------------------------------------------------------------------------------------------------------------------------------------------------

--To to switch from one branch to another 

# git checkout branchname

------------------------------------------------------------------------------------------------------------------------------------------------------

--To merge one branch with another 

# git merge branchname








 
